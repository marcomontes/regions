%h3 MUNICIPIOS

%table.table.table-bordered.table-sm
  %thead
    %tr
      %th CÓDIGO
      %th NOMBRE
      %th ESTADO
      %th REGIONES
      %th{colspan: 3} OPCIONES

  %tbody
    - @municipalities.each do |municipality|
      %tr
        %td= municipality.numeric_code
        %td= municipality.name
        %td= municipality.get_state
        %td= municipality.regions
        %td= link_to 'Ver', municipality
        %td= link_to 'Editar', edit_municipality_path(municipality)
        %td= link_to 'Eliminar', municipality, method: :delete, data: { confirm: 'Está seguro?' }

%br

= link_to 'Nuevo Municipio', new_municipality_path, class: 'btn btn-success btn-sm'